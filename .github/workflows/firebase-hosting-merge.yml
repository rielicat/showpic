# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: 'echo "{\"apiKey\": \"$FIREBASE_API_KEY\",\"authDomain\": \"$FIREBASE_AUTH_DOMAIN\",\"projectId\": \"$FIREBASE_PROJECT_ID\",\"storageBucket\":\"$FIREBASE_STORAGE_BUCKET\",\"messagingSenderId\": \"$FIREBASE_MESSAGING_SENDER_ID\",\"appId\":\"$FIREBASE_APP_ID\"}" > src/firebase.config.json && npm i && npm run build'
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_INSTAGRAM_A76BB }}'
          channelId: live
          projectId: instagram-a76bb