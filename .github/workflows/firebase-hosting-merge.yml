# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run:  echo ${{ secrets.FIREBASE_SERVICE_ACCOUNT_INSTAGRAM_A76BB }} && echo ${{ secrets.FIREBASE_SERVICE_ACCOUNT_INSTAGRAM_A76BB }} > src/firebase.config.json && npm i && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_INSTAGRAM_A76BB }}'
          channelId: live
          projectId: instagram-a76bb
